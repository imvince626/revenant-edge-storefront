---
import type { z } from "zod";
import type { MoneyV2Result } from "../utils/schemas";
import Money from "./Money.svelte";

export interface Props {
  price?: z.infer<typeof MoneyV2Result>;
  title: string;
}
const { price, title } = Astro.props as Props;
---

<div>
  <p class="nav-label mb-4 text-muted">App Exclusive</p>
  <div class="flex items-start justify-between gap-6">
    <h1 class="font-sans text-[1rem] font-semibold leading-snug text-ink">
      {title}
    </h1>

    {price && (
      <p class="shrink-0 font-sans text-sm font-semibold text-ink">
        <Money price={price} showCurrency={false} client:load />
      </p>
    )}
  </div>
</div>
